# Configuring minimum release age across npm, pnpm, yarn, and bun

A minimum release age (or "cooldown") instructs a package manager to ignore any dependency version published less than a configured amount of time ago. Most malicious releases are reported and removed from the registry within a few hours of publication, so even a 24-hour delay screens out the majority of compromised versions before they reach a local install.<sup><a href="#ref-1">[1]</a></sup>

Each major Node.js package manager now supports this configuration, though the setting name and the unit of time differ in each one. The reference below was assembled from Matteo Collina's original gist covering npm, pnpm, and Yarn,<sup><a href="#ref-1">[1]</a></sup> Tom Ricci's expansion adding Bun,<sup><a href="#ref-2">[2]</a></sup> and the official documentation for each tool.<sup><a href="#ref-3">[3]</a></sup><sup><a href="#ref-4">[4]</a></sup><sup><a href="#ref-5">[5]</a></sup><sup><a href="#ref-6">[6]</a></sup>

## Table of Contents

## Quick reference

- **npm**: `min-release-age`, in **days**. Supported in npm CLI **11.10.0** (February 2026).<sup><a href="#ref-3">[3]</a></sup>
- **pnpm**: `minimumReleaseAge`, in **minutes**. Supported in pnpm **10.16** (September 2025). The default became `1440` in pnpm **11.0**.<sup><a href="#ref-4">[4]</a></sup>
- **Yarn**: `npmMinimalAgeGate`, in **minutes**. Supported in Yarn Berry **4.10.0**.<sup><a href="#ref-5">[5]</a></sup>
- **Bun**: `minimumReleaseAge`, in **seconds**. Supported in Bun **1.3.0**.<sup><a href="#ref-6">[6]</a></sup>

## Caveats

Several aspects of the cooldown's behavior can cause it to be silently bypassed. They are documented here, ahead of the per-tool configuration, because each one is a common source of misconfiguration.

### The check runs at install time

The cooldown is enforced when the package manager resolves and installs a version, not when an automation suggests one. Renovate and Dependabot will open update PRs regardless of any cooldown configured in the package manager itself, so their own cooldown options must be set independently. Renovate 42's `config:best-practices` preset enables a three-day cooldown for npm by default.<sup><a href="#ref-1">[1]</a></sup> Dependabot exposes the equivalent control as `cooldown.default-days`.

### A repository pinning an older package manager disables the gate

If a project pins its package manager via the `packageManager` field in `package.json`, Corepack, Volta, a Docker base image, or a CI action such as `setup-node` with `version-file`, the pinned version is the one that performs installs. When the pinned version predates cooldown support, the setting has no effect, regardless of the globally installed package manager or any global configuration. An older binary has no concept of the option and ignores it.

This is the most common reason for a cooldown to be present in configuration but absent in effect. The version that actually performs the install can be confirmed by running `pnpm --version` (or the equivalent for npm, yarn, or bun) from inside the repository, or by inspecting the `setup-node` step in the CI logs.

### A repository's looser configuration overrides a stricter user configuration

Project-level configuration takes precedence over user-level configuration in all four tools. A user-wide cooldown of one day does not apply inside a repository whose `.npmrc`, `pnpm-workspace.yaml`, `.yarnrc.yml`, or `bunfig.toml` sets a smaller value or no value at all. The setting behaves as project policy rather than as a per-user safety preference. A global value, therefore, cannot serve as a backstop for repositories that have not opted in.

### No per-registry scope

None of the four tools support applying the gate to one registry but not another. Internal packages from a private registry are subject to the cooldown unless explicitly listed in the per-tool exclusion option. npm does not yet expose an exclusion option; an issue is open against the CLI tracking it.<sup><a href="#ref-7">[7]</a></sup>

### Defaults are likely to shift

pnpm 11 enables the gate by default at 1440 minutes, and npm CLI v12 is expected to do the same.<sup><a href="#ref-1">[1]</a></sup> Setting the value explicitly today preserves the current configured behavior when the upstream defaults change.

## Per-project configuration

Per-project configuration is committed to the repository and applies to every developer and CI run that uses a supported package manager version. The selected tab is remembered across the rest of the post.

<PackageManagerTabs>
<PackageManagerTab manager="npm">

The setting belongs in the project's `.npmrc`:

```INI filename=".npmrc"
min-release-age=1
```

The value is in days. The setting cannot be combined with `--before` in the same invocation; npm raises an error if both are provided. An npm CLI version that does not recognize the key emits the following warning, which is cosmetic and does not prevent the setting from applying on a supporting CLI:

```text
npm warn Unknown project config "min-release-age". This will stop working in the next major version of npm.
```

</PackageManagerTab>
<PackageManagerTab manager="pnpm">

Since pnpm 11, the canonical location is `pnpm-workspace.yaml`:

```yaml filename="pnpm-workspace.yaml"
minimumReleaseAge: 1440
```

Internal packages that should bypass the gate can be listed under `minimumReleaseAgeExclude`:

```yaml filename="pnpm-workspace.yaml"
minimumReleaseAge: 1440
minimumReleaseAgeExclude:
  - '@platformatic/*'
```

On pnpm 10.x, the same setting is written in `.npmrc` with a different casing:

```INI filename=".npmrc"
minimum-release-age=1440
```

A known bug on pnpm 10.x causes `minimumReleaseAge` to be silently ignored when any package in the workspace sets `shared-workspace-lockfile=false` in its `.npmrc`.<sup><a href="#ref-8">[8]</a></sup> On 10.x, this is worth verifying before assuming the setting is honored.

</PackageManagerTab>
<PackageManagerTab manager="yarn">

The setting belongs in `.yarnrc.yml` (Yarn Berry 4.10 or later):

```yaml filename=".yarnrc.yml"
npmMinimalAgeGate: 1440
```

Packages that should bypass the gate can be listed under `npmPreapprovedPackages`:

```yaml filename=".yarnrc.yml"
npmMinimalAgeGate: 1440
npmPreapprovedPackages:
  - '@platformatic/*'
```

`npmPreapprovedPackages` accepts both glob patterns and exact locators (for example, `@aws-sdk/types@3.877.0`), giving it more granularity than pnpm's package-name-only exclude list.

The documentation states that a duration suffix such as `"7d"` is accepted, but a known parsing bug causes the suffix to be dropped.<sup><a href="#ref-9">[9]</a></sup> Until that is resolved, a plain minute count is the reliable form: one day is `npmMinimalAgeGate: 1440`.

</PackageManagerTab>
<PackageManagerTab manager="bun">

The setting belongs under `[install]` in `bunfig.toml`:

```toml filename="bunfig.toml"
[install]
minimumReleaseAge = 86400
```

The value is in seconds. One day is `86400`.

Packages that should bypass the gate can be listed under `minimumReleaseAgeExcludes`:

```toml filename="bunfig.toml"
[install]
minimumReleaseAge = 86400
minimumReleaseAgeExcludes = ["@types/node", "typescript"]
```

</PackageManagerTab>
</PackageManagerTabs>

## User-wide configuration

User-wide configuration applies on a single machine, across every repository that does not override it. As noted in the caveats above, project-level configuration takes precedence over the values shown here.

<PackageManagerTabs>
<PackageManagerTab manager="npm">

```bash
npm config set min-release-age 1 --location=user
```

This writes to `~/.npmrc`. Substituting `global` for `user` writes to the global npm configuration instead.

</PackageManagerTab>
<PackageManagerTab manager="pnpm">

```bash
pnpm config set minimumReleaseAge 1440 --location=user
```

On pnpm 11 and later, non-auth settings written this way are stored in `~/.config/pnpm/config.yaml` rather than `~/.npmrc`.

</PackageManagerTab>
<PackageManagerTab manager="yarn">

```bash
yarn config set --home npmMinimalAgeGate 1440
```

`--home` writes to `~/.yarnrc.yml` rather than the project-local file.

</PackageManagerTab>
<PackageManagerTab manager="bun">

Bun reads project-level `bunfig.toml` only; user-wide configuration is not yet supported. An open pull request adds the corresponding behavior.<sup><a href="#ref-10">[10]</a></sup> Once merged, the same key written in a user-wide `bunfig.toml` will apply across projects.

</PackageManagerTab>
</PackageManagerTabs>

## References

<ol>
  <li id="ref-1">Matteo Collina, <a href="https://gist.github.com/mcollina/b294a6c39ee700d24073c0e5a4e93104">"Configuring minimum release age across npm, pnpm, and yarn"</a> (gist, April 2026). Original write-up covering npm, pnpm, and Yarn, including the install-time vs. PR-time distinction and the expected default shifts.</li>
  <li id="ref-2">Tom Ricci, <a href="https://gist.github.com/tom-ricci/bade55606ebb7a550598f11e29f1d6b0">"Configuring minimum release age across npm, pnpm, yarn, and bun"</a> (gist). Extends Collina's notes with Bun coverage, version metadata, and the per-tool exclusion options.</li>
  <li id="ref-3">npm CLI documentation, <a href="https://docs.npmjs.com/cli/v11/using-npm/config#min-release-age"><code>min-release-age</code></a>.</li>
  <li id="ref-4">pnpm documentation, <a href="https://pnpm.io/settings#minimumreleaseage"><code>minimumReleaseAge</code></a>.</li>
  <li id="ref-5">Yarn documentation, <a href="https://yarnpkg.com/configuration/yarnrc#npmMinimalAgeGate"><code>npmMinimalAgeGate</code></a>.</li>
  <li id="ref-6">Bun documentation, <a href="https://bun.sh/docs/runtime/bunfig#install-minimumreleaseage"><code>install.minimumReleaseAge</code></a>.</li>
  <li id="ref-7">npm CLI issue <a href="https://github.com/npm/cli/issues/8994">#8994</a>. Tracks an exclusion list for <code>min-release-age</code>.</li>
  <li id="ref-8">pnpm issue <a href="https://github.com/pnpm/pnpm/issues/10008">#10008</a>. <code>minimumReleaseAge</code> silently ignored on pnpm 10.x when any workspace package sets <code>shared-workspace-lockfile=false</code>.</li>
  <li id="ref-9">Yarn issue <a href="https://github.com/yarnpkg/berry/issues/6991">#6991</a>. Duration suffixes silently dropped when parsing <code>npmMinimalAgeGate</code>.</li>
  <li id="ref-10">Bun pull request <a href="https://github.com/oven-sh/bun/pull/28727">#28727</a>. Adds user-wide <code>bunfig.toml</code> support.</li>
</ol>

{
  "name": "blog-posts",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/blog-posts/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "nx:noop",
      "dependsOn": ["lint:eslint", "lint:vale"]
    },
    "lint:eslint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:vale": {
      "command": "mise exec -- vale --minAlertLevel=error src/lib/posts",
      "options": {
        "cwd": "{projectRoot}"
      }
    }
  }
}

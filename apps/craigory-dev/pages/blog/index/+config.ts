export default {
  desc: 'Posts on developer tooling, monorepos, git, and GitHub, plus reviews of tiki bars.',
  // Every page of the pagination shares one card — see `+image.ts` — so the alt
  // text describes that card rather than the page it is attached to. Without
  // this, `renderer/+imageAlt.ts` would derive "Blog, page 2" from the title and
  // misdescribe artwork that reads "Blog".
  imageAlt: 'Blog',
};
